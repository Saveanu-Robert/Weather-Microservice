# WeatherAPI.com API Key
# Get your free API key from: https://www.weatherapi.com/signup.aspx
# Required for both local development and Docker deployment
WEATHER_API_KEY=your-api-key-here

# Database Configuration (optional, defaults provided)
DATABASE_URL=jdbc:h2:file:./data/weatherdb
DATABASE_USERNAME=sa
DATABASE_PASSWORD=

# Spring Profile
# For local development: Use "dev,local" to load application-local.yml
# For Docker: Docker Compose overrides this to "dev" only
# Valid values: dev, test, prod
SPRING_PROFILES_ACTIVE=dev,local
